pool:
  name: default
  # Inclusive range of addresses handed to LoadBalancer services. Must be free
  # on the subnet the nodes sit on, since announcement is ARP based.
  start: "192.168.1.60"
  stop: "192.168.1.98"

l2Announcements:
  interfaces:
    - ^eth[0-9]+
  announceOnControlPlane: false
