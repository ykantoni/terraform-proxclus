---
- name: update initramfs
  ansible.builtin.command: update-initramfs -u -k all
  become: true
  changed_when: true

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
