# Task runner for this stack only. Independent of the root Justfile: run
# these from inside postgres/, against postgres/'s own state.

apply:
    terraform apply -auto-approve

destroy:
    terraform destroy -auto-approve

fmt:
    terraform fmt -recursive
